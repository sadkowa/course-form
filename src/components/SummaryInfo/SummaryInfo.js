import React from 'react'
import StyledSummaryInfo from './SummaryInfo.styled'

const SummaryInfo = ({ children }) => {
    return <StyledSummaryInfo>{children}</StyledSummaryInfo>
}

export default SummaryInfo